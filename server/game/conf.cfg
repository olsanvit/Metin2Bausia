# ============================================================
# Metin2Bausia — Game server CH1 konfigurace (TMP4)
# UPRAVIT před nasazením: PUBLIC_IP na skutečnou IP/doménu QNAP
# ============================================================

# Identifikace
CHANNEL             = 1
HOSTNAME            = bausia-ch1
PORT                = 13000
P2P_PORT            = 13900

# Veřejná IP (klient se připojuje na tuto adresu) — ZMĚNIT!
PUBLIC_IP           = 0.0.0.0

# DB server (interní docker síť)
DB_ADDR             = mt2-db
DB_PORT             = 15000

# Auth server (interní docker síť)
AUTH_ADDR           = mt2-auth
AUTH_PORT           = 11002

# Mapy povolené na CH1
MAP_ALLOW           = 1 21 22 23 24 25 26 27 41 42 43 44 45 46 61 62 63 64 65 66

# Rate modifikátory (1 = standard)
EXP_RATE            = 1
ITEM_DROP_RATE      = 1
YANG_DROP_RATE      = 1
ORE_DROP_RATE       = 1

# Hráči
MAX_PLAYERS_PER_SERVER = 1000
DEFAULT_VIEW_DIST   = 5000

# PvP (0 = vypnuto)
PVP_MODE            = 0

# Cesty ke sdíleným datům
LOCALE_PATH         = /usr/local/metin2/share/locale
MAP_PATH            = /usr/local/metin2/share/maps
ITEM_PROTO_FILE     = /usr/local/metin2/share/item_proto/item_proto
MOB_PROTO_FILE      = /usr/local/metin2/share/mob_proto/mob_proto
SKILL_PROTO_FILE    = /usr/local/metin2/share/skill_proto
QUEST_PATH          = /usr/local/metin2/share/scripts/quest

# Log (0 = minimum, 3 = debug)
LOG_LEVEL           = 0
